//map array function returns new array with recalculated values

const numbers = [10, 20, 30];
const doubledNumbers = numbers.map(elem => elem * 2);
console.log(numbers);
console.log(doubledNumbers);
